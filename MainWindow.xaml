<Window x:Class="PomodoroWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PomodoroWPF"
        xmlns:conv="clr-namespace:PomodoroWPF.Converters"
        Title="番茄钟"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        Background="{StaticResource BgBrush}"
        Closing="Window_Closing"
        SourceInitialized="Window_SourceInitialized">

    <Window.InputBindings>
        <KeyBinding Key="F11" Command="{Binding ToggleFullscreenCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding ToggleFullscreenCommand}"/>
        <KeyBinding Key="Q" Modifiers="Control" Command="{Binding QuitCommand}"/>
    </Window.InputBindings>

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid x:Name="RootGrid" Background="{StaticResource BgBrush}">

        <!-- ==================== 首页 ==================== -->
        <Grid Visibility="{Binding IsHomeVisible, Converter={StaticResource BoolToVis}}">
            <!-- 右上角提示 -->
            <TextBlock Text="F11/ESC 切换全屏  ·  Ctrl+Q 退出"
                       FontFamily="Segoe UI" FontSize="11"
                       Foreground="{StaticResource TextMutedBrush}"
                       HorizontalAlignment="Right" VerticalAlignment="Top"
                       Margin="0,18,24,0"/>

            <!-- 中央时钟区域 -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                        Margin="0,-80,0,0">
                <TextBlock Text="{Binding Home.CurrentTime}"
                           FontFamily="Segoe UI" FontSize="130" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Home.CurrentDate}"
                           FontFamily="Segoe UI" FontSize="20"
                           Foreground="{StaticResource TextDimBrush}"
                           HorizontalAlignment="Center" Margin="0,6,0,0"/>
                <TextBlock Text="{Binding CurrentTaskDisplay}"
                           FontFamily="Segoe UI" FontSize="18"
                           Foreground="{StaticResource AccentBrush}"
                           HorizontalAlignment="Center" Margin="0,28,0,0"/>
                <TextBlock Text="{Binding Home.TodayStatsText}"
                           FontFamily="Segoe UI" FontSize="14"
                           Foreground="{StaticResource TextDimBrush}"
                           HorizontalAlignment="Center" Margin="0,12,0,0"/>
                <TextBlock Text="{Binding Home.StreakText}"
                           FontFamily="Segoe UI" FontSize="14" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}"
                           HorizontalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Text="{Binding Home.TaskSummaryText}"
                           FontFamily="Segoe UI" FontSize="12"
                           Foreground="{StaticResource TextDimBrush}"
                           HorizontalAlignment="Center" Margin="0,8,0,0"/>
                <!-- Daily Goal -->
                <Border Background="{StaticResource CardBrush}" CornerRadius="6"
                        HorizontalAlignment="Center" Padding="16,8" Margin="0,12,0,0"
                        MinWidth="200">
                    <StackPanel>
                        <TextBlock Text="{Binding Home.DailyGoalText}"
                                   FontFamily="Segoe UI" FontSize="12"
                                   Foreground="{StaticResource TextDimBrush}"
                                   HorizontalAlignment="Center"/>
                        <ProgressBar Value="{Binding Home.DailyGoalProgress}" Maximum="1"
                                     Height="6" Margin="0,6,0,0"
                                     Foreground="{StaticResource AccentBrush}"
                                     Background="{StaticResource TextMutedBrush}"
                                     BorderThickness="0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 右下角按钮 -->
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom"
                        Margin="0,0,40,40">
                <local:RoundButton Text="任 务 列 表" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="13" BtnWidth="170" BtnHeight="48"
                                   Command="{Binding OpenTaskListCommand}" Margin="0,0,0,14"/>
                <local:RoundButton Text="统 计" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="13" BtnWidth="170" BtnHeight="48"
                                   Command="{Binding OpenStatsCommand}" Margin="0,0,0,14"/>
                <local:RoundButton Text="设 置" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="13" BtnWidth="170" BtnHeight="48"
                                   Command="{Binding OpenSettingsCommand}" Margin="0,0,0,14"/>
                <local:RoundButton Text="倒 计 时" BgColor="#f59e0b" FgColor="#000000"
                                   FontSize="14" BtnWidth="170" BtnHeight="52"
                                   Command="{Binding NavigateToCountdownCommand}" Margin="0,0,0,14"/>
                <local:RoundButton Text="正 计 时" BgColor="#10b981" FgColor="#000000"
                                   FontSize="14" BtnWidth="170" BtnHeight="52"
                                   Command="{Binding NavigateToStopwatchCommand}" Margin="0,0,0,14"/>
                <local:RoundButton Text="退 出" BgColor="#292524" FgColor="#57534e"
                                   FontSize="11" BtnWidth="170" BtnHeight="36"
                                   Command="{Binding QuitCommand}" Margin="0,18,0,0"/>
            </StackPanel>
        </Grid>

        <!-- ==================== 倒计时页 ==================== -->
        <Grid Visibility="{Binding IsCountdownVisible, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4.5*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <!-- Row 0: 左上返回 + 右上任务 -->
            <Grid Grid.Row="0">
                <local:RoundButton Text="← 返回" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="12" BtnWidth="90" BtnHeight="38"
                                   Command="{Binding NavigateToHomeCommand}"
                                   HorizontalAlignment="Left" VerticalAlignment="Top"
                                   Margin="20,20,0,0"/>
                <local:RoundButton Text="任 务" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="12" BtnWidth="90" BtnHeight="38"
                                   Command="{Binding OpenTaskListCommand}"
                                   HorizontalAlignment="Right" VerticalAlignment="Top"
                                   Margin="0,20,20,0"/>
            </Grid>

            <!-- Row 1: 模式标识 + 周期指示 + 进度环 -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="CdModeLabel" Grid.Row="0" Text="{Binding Countdown.ModeLabel}"
                           FontFamily="Segoe UI" FontSize="16" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Grid.Row="1" Text="{Binding Countdown.CycleDots}"
                           FontFamily="Segoe UI" FontSize="18"
                           Foreground="{StaticResource TextDimBrush}"
                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
                <Viewbox Grid.Row="2" Stretch="Uniform"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Margin="20,0,20,10">
                    <local:ProgressRing x:Name="CdRing" Size="400" RingWidth="8"/>
                </Viewbox>
            </Grid>

            <!-- Row 2: 分钟设置 -->
            <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="6"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Padding="18,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="分钟" FontFamily="Segoe UI" FontSize="12"
                               Foreground="{StaticResource TextDimBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="CdMinutesInput" Text="{Binding Countdown.InputMinutes, UpdateSourceTrigger=PropertyChanged}"
                             FontFamily="Segoe UI" FontSize="13"
                             Background="Black" Foreground="{StaticResource TextBrush}"
                             CaretBrush="{StaticResource AccentBrush}"
                             BorderThickness="0" Width="50"
                             VerticalContentAlignment="Center" Padding="4,2"/>
                    <local:RoundButton Text="应 用" BgColor="#f59e0b" FgColor="#000000"
                                       FontSize="12" BtnWidth="70" BtnHeight="34"
                                       Command="{Binding Countdown.ApplyMinutesCommand}" Margin="8,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- Row 3: 任务标签 -->
            <TextBlock Grid.Row="3" Text="{Binding CurrentTaskDisplay}"
                       FontFamily="Segoe UI" FontSize="13"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- Row 4: 控制按钮 -->
            <StackPanel Grid.Row="4" Orientation="Horizontal"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                <local:RoundButton Text="开 始" BgColor="#f59e0b" FgColor="#000000"
                                   FontSize="14" BtnWidth="130" BtnHeight="48"
                                   Command="{Binding Countdown.StartCommand}" Margin="6"/>
                <local:RoundButton Text="暂 停" BgColor="#292524" FgColor="#57534e"
                                   FontSize="14" BtnWidth="130" BtnHeight="48"
                                   Command="{Binding Countdown.PauseCommand}" Margin="6"/>
                <local:RoundButton Text="重 置" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="13" BtnWidth="100" BtnHeight="42"
                                   Command="{Binding Countdown.ResetCommand}" Margin="6"/>
            </StackPanel>
        </Grid>

        <!-- ==================== 正计时页 ==================== -->
        <Grid Visibility="{Binding IsStopwatchVisible, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <!-- Row 0: 左上返回 + 右上任务 -->
            <Grid Grid.Row="0">
                <local:RoundButton Text="← 返回" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="12" BtnWidth="90" BtnHeight="38"
                                   Command="{Binding NavigateToHomeCommand}"
                                   HorizontalAlignment="Left" VerticalAlignment="Top"
                                   Margin="20,20,0,0"/>
                <local:RoundButton Text="任 务" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="12" BtnWidth="90" BtnHeight="38"
                                   Command="{Binding OpenTaskListCommand}"
                                   HorizontalAlignment="Right" VerticalAlignment="Top"
                                   Margin="0,20,20,0"/>
            </Grid>

            <!-- Row 1: 进度环 -->
            <Viewbox Grid.Row="1" Stretch="Uniform"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Margin="20,10">
                <local:ProgressRing x:Name="SwRing" Size="400" RingWidth="8"
                                    RingColor="#10b981"/>
            </Viewbox>

            <!-- Row 2: 任务标签 -->
            <TextBlock Grid.Row="2" Text="{Binding CurrentTaskDisplay}"
                       FontFamily="Segoe UI" FontSize="13"
                       Foreground="{StaticResource SuccessBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- Row 3: 控制按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                <local:RoundButton Text="开 始" BgColor="#10b981" FgColor="#000000"
                                   FontSize="14" BtnWidth="130" BtnHeight="48"
                                   Command="{Binding Stopwatch.StartCommand}" Margin="6"/>
                <local:RoundButton Text="暂 停" BgColor="#292524" FgColor="#57534e"
                                   FontSize="14" BtnWidth="130" BtnHeight="48"
                                   Command="{Binding Stopwatch.PauseCommand}" Margin="6"/>
                <local:RoundButton Text="重 置" BgColor="#0a0a0a" FgColor="#57534e"
                                   FontSize="13" BtnWidth="100" BtnHeight="42"
                                   Command="{Binding Stopwatch.ResetCommand}" Margin="6"/>
            </StackPanel>
        </Grid>

    </Grid>
</Window>
